// Este archivo no debe usarse. El backend real está en /backend/server.js

// Archivo deshabilitado para evitar conflictos con el backend principal.

// Si necesitas un backend, usa el de la carpeta /backend